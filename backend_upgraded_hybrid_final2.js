onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
onst credentialsPath = path.join(__dirname, 'gcloud_sa.pem'); 
      try { 
        fs.writeFileSync(credentialsPath, gac); 
        googleCredentialsPath = credentialsPath; 
        console.log('✅ Google PEM credentials saved to file'); 
      } catch (error) { 
        console.error('❌ Failed to save Google PEM credentials:', error.message); 
      } 
    } else 
